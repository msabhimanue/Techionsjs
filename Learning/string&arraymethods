# String Methods
Javascript strings are primitive and immutable: All string methods produce a new string without altering the original string.
Common String Methods

let text = "JavaScript is amazing!";

// Get string length
console.log(text.length); // Output: 23

// Convert to uppercase
console.log(text.toUpperCase()); // Output: JAVASCRIPT IS AMAZING!

// Extract substring
console.log(text.slice(0, 10)); // Output: JavaScript

// Replace substring
console.log(text.replace("amazing", "awesome")); // Output: JavaScript is awesome!

// Check if a string includes another string
console.log(text.includes("amazing")); // Output: true

# Array Methods
Common Array Methods

let numbers = [1, 2, 3, 4, 5];

// Add elements
numbers.push(6); // Adds to the end
numbers.unshift(0); // Adds to the beginning
console.log(numbers); // Output: [0, 1, 2, 3, 4, 5, 6]

// Remove elements
numbers.pop(); // Removes the last element
numbers.shift(); // Removes the first element
console.log(numbers); // Output: [1, 2, 3, 4, 5]

// Map: Transform array elements
let squares = numbers.map((num) => num * num);
console.log(squares); // Output: [1, 4, 9, 16, 25]

// Filter: Keep even numbers
let evens = numbers.filter((num) => num % 2 === 0);
console.log(evens); // Output: [2, 4]